import 'package:flutter/material.dart';

class AtiviPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(

      appBar: AppBar(
        backgroundColor: Colors.transparent,
        elevation: 0,
        title: Text("Atividade"),
      ),

      backgroundColor: Colors.grey[800],
      body: SafeArea(
        child: Container(
            
        ),
      ),
    );
  }
}